{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}\\start",
            "url":"htt:/localhost:3000"
        }
    ]
}